# Combined central figure

`COI_plots.R` uses the five long-format CSV files in this folder to reproduce the central combined-results figure. From the repository root, run:

```r
source("combined-figure/COI_plots.R")
```

The script writes the 600-dpi output to `combined-figure/Fig1.png`.
